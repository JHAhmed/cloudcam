
export const load = ({ params, url }) => {

    // Get URL params called allowGallery and imagePersistence
    // let allowGallery = url.searchParams.get('allowGallery');
    // let imagePersistence = url.searchParams.get('imagePersistence');

    // Return an object where `images` is a PROMISE, not the final data


    // return {
    //     allowGallery,
    //     imagePersistence
    // };
};